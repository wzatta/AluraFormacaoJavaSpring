package com.cilazatta.vollMed.controler;

import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.cilazatta.vollMed.dto.EnderecoDTO;

@RestController
@RequestMapping(name = "endereco")
public class EnderecoControler {

	public EnderecoDTO cadastrar(@RequestBody EnderecoDTO endereco) {
		System.out.println(endereco.toString());
		return endereco;
	}
	
}
